import React from 'react';
import { FirstCard, LayoutContainer } from 'components/Home';

const Home = () => {
  return (
    <LayoutContainer>
      <FirstCard />
    </LayoutContainer>
  );
};

export default Home;
